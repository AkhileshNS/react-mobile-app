{
  "name": "react-mobile-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@capacitor/android": "^1.3.0",
    "@capacitor/cli": "^1.3.0",
    "@capacitor/core": "^1.3.0",
    "@emotion/core": "^10.0.22",
    "@emotion/styled": "^10.0.23",
    "dayjs": "^1.8.17",
    "elegant-standard": "^2.0.0",
    "immer": "^9.0.6",
    "lodash": "^4.17.15",
    "mobx": "^5.15.0",
    "mobx-react": "^6.1.4",
    "onsenui": "^2.10.10",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-onsenui": "^1.11.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "android": "npm run clean:android && npm run build && npx cap add android",
    "clean": "rimraf ./android && rimraf ./ios && rimraf ./electron",
    "clean:android": "rimraf ./android",
    "clean:iod": "rimraf ./ios",
    "clean:electron": "rimraf ./electron"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "rimraf": "^3.0.0"
  }
}
